'use strict';

angular.module('makeshiftApp')
  .controller('MainCtrl', function ($scope, $rootScope) {
    $rootScope._ = _
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
